# 创建产品文档

创建新产品文档时，使用`ask`和`question`工具，用设问的模式和用户将产品细节沟通清楚。

在你输出文件时，基于主分支创建新的分支，名字为`prd/<需求slug>`，将新建的产品文档放置在`docs/product/draft`，提交commit并`push`

如果环境有使用`pull request`工具，则使用该工具提`pull request`，并将连接提示出来。上述操作完成后要求用户review，并给出pull request链接。
